# Homepage images

Drop these 5 photos here (or upload them to your WordPress media library and
update the `src` paths in `index.html`). Until they exist, each slot shows a
branded fallback block, so the page never looks broken.

| File | Used for | Suggested photo (from the set you shared) |
|------|----------|-------------------------------------------|
| `team-counter.jpg`   | Hero (main) + "Retain" card | Team member smiling at the front counter |
| `team-drivethru.jpg` | Hero (overlap)              | Team members with headsets at the drinks station |
| `team-kitchen.jpg`   | "Engage" card              | Team member at the fryer |
| `team-fries.jpg`     | "Reward" card              | Hand bagging waffle fries |
| `team-service.jpg`   | "What can LEAN365 do?" video poster | Team member carrying a tray |

**Tips**
- Landscape crops around 1600×1000 work well; the hero main is portrait (≈1000×1250).
- Save as optimized JPG/WebP (<300 KB each) for fast loading.
- The LMS screenshot is already wired to its live URL and needs nothing here.
